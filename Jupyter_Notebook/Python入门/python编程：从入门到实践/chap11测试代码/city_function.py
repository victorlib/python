def place(city, country):
    """生成一个地方名"""
    t = city.title()+', '+country.title()
    return t


def place2(city, country, population):
    t = city.title() + ', ' + country.title()+" - population " + str(population)
    return t





