# -*-coding:utf-8 -*-
import os
import shutil
shutil.copyfile('std.py', 'newstd.py')
shutil.move('fibo.py', 'installdir')


print(os.getcwd())  # Return the current working directory
#os.chdir('E:\Google')  # Change current working directory
#os.system('mkdir today')  # Run the command mkdir in the system shell






















































